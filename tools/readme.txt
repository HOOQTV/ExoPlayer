# How to build ExoPlayer JARS for Playermodule#

execute shell command:
./builder.sh

if you get permission error, add permission by using this command:
chmod +x builder.sh
chmod +x jarextractor.sh

the result will be inside folder "outputs"
